<%= form_for(@claim) do |f| %>
  <% if @claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@claim.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% @claim.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :policy_number %><br />
    <%= f.text_field :policy_number %>
  </div>
  <div class="field">
    <%= f.label :date_of_loss %><br />
    <%= f.date_select :date_of_loss %>
  </div>
  <div class="field">
    <%= f.label :location_of_loss %><br />
    <%= f.text_field :location_of_loss %>
  </div>
  <div class="field">
    <%= f.label :claim_description %><br />
    <%= f.text_field :claim_description %>
  </div>
  <div class="field">
    <%= f.label :damage_details %><br />
    <%= f.text_field :damage_details %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
